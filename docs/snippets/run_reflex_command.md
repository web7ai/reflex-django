```bash
python manage.py migrate
python manage.py run_reflex
```

Open **http://localhost:3000/** for the Reflex UI (hot reload).

Open **http://localhost:8000/admin/** for Django admin (or browse admin via :3000 when Vite proxies are active).
