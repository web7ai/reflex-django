# settings.py — optional: proxy Django admin/API to a separate server during dev
# Leave unset when using `manage.py run_reflex` (Django is mounted in the Reflex backend).
# RXDJANGO_PROXY_SERVER = "http://127.0.0.1:8000"